/**
 * @rangamai/shared — public barrel.
 *
 * Domain types + enums (the contract shared by web, admin, and api) and the
 * canonical seed content. Consumers import everything from "@rangamai/shared".
 */
export * from "./types";
export * from "./seed";
